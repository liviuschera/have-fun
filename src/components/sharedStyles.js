import { css } from "styled-components";

export const inputSelectField = css`
   height: 4rem;
   font-family: Roboto;
   font-size: var(--font-paragraph-size);
   border: none;
   padding-left: 0.5rem;
   outline-color: var(--color-primary);
`;
